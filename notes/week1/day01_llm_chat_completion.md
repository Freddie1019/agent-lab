# Day 1 概念卡片：LLM API 与 Chat Completion 协议

## 卡片 1：Chat Completion 协议
- 它是什么：它是 OpenAI 推出的一套结构化对话接口规范（以 messages 数组为核心输入）。由于设计得简单且扩展性极强，它已经成为全球大模型的事实标准。

- 为什么它是事实标准：
- 结构化降低了工程门槛： 早期 text-completion 需要开发者自己用各种特殊符号（如 <|im_start|>）拼 prompt，极易出错。Chat 协议把多轮对话结构化，交给底层自动组装。
- 生态垄断： OpenAI 早期市场份额极大，导致所有开源框架（LangChain、LlamaIndex）和第三方工具都围绕这套 API 编写。其他厂商不想被孤立，就必须主动兼容这套接口。

- 我能用同样的代码切换 OpenAI / DeepSeek / 通义吗？为什么？
- 能。 因为 DeepSeek、通义千问（Qwen）等主流大厂的 API 都在接口层做了 OpenAI 兼容（OpenAI-Compatible）。你只需要在代码中更换 api_key 和 base_url（改个配置结构），不需要重构业务逻辑代码，就能实现完美无缝切换。

## 卡片 2：messages 数组
- 用一句话说清楚 messages 数组在 LLM 调用里的角色：
- 它是一个时间线包裹，把过去的对话历史（谁在什么时间说了什么）按顺序打包，作为大模型预测下一个 Token 的上下文输入。

- LLM 真的有"记忆"吗？它的"记忆"实际来自哪里？
- 没有。 LLM 本质上是无状态的（Stateless）。它像一个没有短期记忆的解题机器，每次 API 调用都是独立的。它的“记忆”完全来自你每次请求时雷打不动、重新发给它的 messages 历史数组。

- 一次调用要传完整历史，这会带来什么问题？（提示：你在任务 4 看到 token 怎么增长的）
- 成本雪崩（Token 爆炸）： 第 1 轮对话 100 token，第 10 轮时你得把前 9 轮的 token 全带上，每次请求都在为历史数据重复买单，Token 消耗呈等差数列（线性和阶梯式）爆发增长。
- 性能下降（延迟变高）： 输进大模型的 Context（上下文）越长，模型计算第一个字（First Token Time）的耗时就越久。
- 触及上下文窗口上限： 聊得太深会超出模型最大 Token 限制（比如 8k 或 32k），导致模型“失忆”或报错拒绝回答。


## 卡片 3：三种角色
- system 的作用：全局总导演。用来设定 AI 的人设（如“你是一个资深 Python 专家”）、行为边界、输出格式（如“必须返回 JSON”），它的权重通常比 user 更高，起兜底控制作用。
- user 的作用：最终人类用户。负责提出具体的问题、任务指令或当前需要处理的输入数据。
- assistant 的作用：大模型自己。代表 AI 之前生成过的回答。在多轮对话中，必须把 AI 之前的回答标记为 assistant 喂回去，模型才知道自己说过什么。

- 思考：为什么要分这三种角色？合并成一个 prompt 字符串不行吗？
- 不行（或效果极差）。

- 防止权限注入（Prompt Injection）： 如果混在一个字符串里，模型很难分清什么是“开发者给的绝对命令（System）”，什么是“恶意用户好玩的调侃（User）”。分开角色后，模型在底层注意力机制（Attention）上会天然给予 system 更高的防线权限。

- 结构化清晰度： 分开后，少样本提示（Few-shot）极其好写，你可以直接在 messages 里伪造几轮 user / assistant 的对话来调教模型，而不用拿奇怪的标点符号去肉眼区分。

## 卡片 4：核心参数
| 参数 |                       作用                                                    |                                            Agent 场景该怎么设                                                             |
|------|------------------------------------------------------------------------------|--------------------------------------------------------------------------------------------------------------------------|
| temperature | 控制输出的随机性与创造力。越接近 0 越严谨、死板；越接近 1 越发散、有创造力。 | 设为 0（或极低如 0.1）。 Agent 往往涉及复杂的工具调用（Tool Calling）和逻辑推理，需要极高的确定性和严格遵循指令，不能让它“瞎编”。 |
| max_tokens | 限制模型单次输出（Reply）的最大 Token 数量，防止模型废话太多或陷入死循环。 | 根据任务按需设中等偏高值（如 2048-4096）。 Agent 思考过程（Thought）和调用工具的参数可能很长，设太小会导致输出截断，Agent 直接崩溃。 |
| stream | 是否开启流式传输。开启后像打字机一样一个字一个字吐数据，不开启则憋大招一次性返回。| 视情况而定。 如果是 Agent 内部的思考、链式调用（LLM 调 LLM），设为 False 方便后台直接解析结构化数据；如果要将 Agent 的思考过程同步给用户看，设为 True 以缓解用户焦虑。 |

## 卡片 5：返回结构
- `choices[0].message.content`：模型最终生成的纯文本回复内容
- `finish_reason` stop：正常结束。模型觉得自己说完了，完美收尾。

- length：被截断。达到了你设置的 max_tokens 上限，被迫闭嘴。

- tool_calls：触发了工具调用。模型说：“我先不答了，请帮我调个外部函数/API”。

- content_filter：敏感词拦截。触发了安全机制，输出被掐断。
- `usage` 里的 token 数有什么用？
- 算账（计费）： 输入（prompt_tokens）和输出（completion_tokens）价格不同，这是你给云厂商交钱的唯一凭证。

- 工程工程化控制： 监控当前的上下文长度，当发现总 token 快接近模型上限时，程序好及时启动“历史裁剪”或“总结摘要”算法，防止下一次调用溢出。

## 卡片 6：流式 vs 非流式
- 总耗时差不多，为什么流式体验好这么多？
- 因为它极大地降低了首字延迟（TTFT, Time to First Token）。人肉眼阅读的速度是有限的，非流式需要等 10 秒憋出整段话，用户会觉得卡死；流式在 0.5 秒内就吐出第一个字，用户可以边吐边读，在心理学上消除了“等待焦虑”，把主观等待时间降到了最低。
- 流式模式下怎么知道 token 用量？（思考题，可以查文档）
- 在标准的 Chat Completion 协议中，你可以在请求时传入一个参数：stream_options={"include_usage": true}。

- 这样一来，流式返回的 最后一个 Chunk（数据块） 或者倒数第二个 Chunk 中，就会包含一个完整的 usage 字段，里面记录了这次流式交互的总 Token 消耗，不需要你在前端自己肉眼去数或者用 tiktoken 库盲猜。
